# Round18 Execution Summary

Date: 2026-05-09 Australia/Sydney

## Completed

### Batch 0/1: Contract, Guard, Smoke

Completed:

- isolated `round18/` workspace;
- execution contract;
- manifest schema;
- strict guard;
- manifest validator;
- raw JSON smoke run;
- repository-wide suspicious-token audit;
- Batch 2 task templates.

Verification:

- `PYTHONPATH=. pytest -q round18/tests` -> `4 passed`.
- all current run manifests validate.
- all current run manifests pass strict guard.

### Batch 2 Sparse

Completed:

- O-S1 BM25 lexical baseline.
- O-S2 structured sparse routing.
- O-S3 feedback expansion.
- O-S4 sparse fusion.

Best single sparse source:

- O-S1 BM25 top500 macro recall: `0.5861471861471862`.

Best fixed strict sparse fusion:

- O-S4 `strict_rrf_prf_heavy`
- top100 macro recall: `0.4567099567099567`
- top500 macro recall: `0.6195887445887446`
- hit-any@500: `0.8506493506493507`

Status:

- approved as strict sparse candidate.

### Batch 2 Dense-In-Sparse

Completed:

- O-D1 light bi-encoder lane with deterministic fallback.
- O-D3 bounded cross-encoder lane.

O-D1 result:

- fallback active: `sklearn_tfidf_svd`
- not evidence for true semantic bi-encoder quality.
- top100 macro recall dropped to `0.3496753246753247`.
- approved only as strict fallback baseline.

O-D3 result:

- model: `cross-encoder/ms-marco-MiniLM-L6-v2`
- scoring scope: sparse top64 only.
- full dev top64:
  - macro recall@3: `0.2159090909090909`
  - macro recall@10: `0.3029220779220779`
  - macro recall@64: `0.4140692640692641`
- approved as strict dense/neural leaf.

### Batch 2 Aggregator

Completed:

- O-A2 fixed-rule leaf-flat aggregation.

Recommended strict evidence top3 candidate:

```text
round18/outputs/o_aggregate/o_a2_calibrated_rank/dev_full_dev_o_a2_calibrated_rank_strict_ce64_sparse_backfill_top500_candidates.json
```

Metrics:

- macro recall@3: `0.2159090909090909`
- macro recall@10: `0.3029220779220779`
- macro recall@100: `0.4567099567099567`
- macro recall@500: `0.6195887445887446`

Secondary strict candidate:

```text
round18/outputs/o_aggregate/o_a2_calibrated_rank/dev_full_dev_o_a2_calibrated_rank_strict_rrf_sparse_ce_top500_candidates.json
```

Metrics:

- macro recall@1: `0.10995670995670996`
- macro recall@3: `0.18625541125541126`
- macro recall@10: `0.2937229437229437`
- macro recall@100: `0.4567099567099567`
- macro recall@500: `0.6195887445887446`

## Current Scientific Status

H1: sparse fusion beats a single sparse gate.

- Supported.
- O-S4 fixed fusion improves top500 macro recall from `0.5861` to `0.6196`.

H2: dense scoring inside sparse candidates improves sparse-only ranking.

- Supported for early ranks.
- O-A2 CE backfill improves top3 macro recall from `0.1123` to `0.2159` while preserving top100/top500.

H3: final leaf-flat aggregator beats individual leaves.

- Partially supported.
- Fixed CE-backfill and RRF variants improve early evidence ranking.
- Train-side selection is still required before strict final freeze.

## Not Completed

Classifier work was attempted but closed without a deliverable.

Reason:

- current pipeline only has dev-side sparse/dense/aggregator artifacts.
- final classifier needs train-side ranking/context artifacts generated by the same pipeline.
- training a classifier only from dev context would violate the split policy.

## Next Required Step

Generate train-side current-run artifacts:

1. O-S1 train full sparse pool.
2. O-S2/O-S3 train variants if needed.
3. O-S4 train sparse fusion using fixed strict weights.
4. O-D3 train cross-encoder top64 rerank inside train sparse candidates.
5. O-A2 train aggregator output with the same fixed strict rule.

Only after that should classifier work resume.

Recommended classifier next task:

- build `O-C1/O-C2` context builder and TF-IDF/logreg baseline from train contexts;
- evaluate frozen classifier on dev contexts;
- keep Transformer classifier for a later wave after baseline works.

## Current Master Verdict

`NEEDS INVESTIGATION BEFORE FINAL PIPELINE APPROVAL`

Reason:

- evidence retrieval path has strong strict candidates;
- classifier path is not yet train/dev complete;
- no clean end-to-end train-to-dev rebuild exists yet;
- notebook/resource path has not been rebuilt.

