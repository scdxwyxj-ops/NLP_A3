# Archive

This directory keeps legacy notebooks and historical experiment artifacts that
are not the current submission path. The current self-contained Colab notebook
lives in `submissions/resource/`, and the current report project lives in
`docs/report/latex/`.
